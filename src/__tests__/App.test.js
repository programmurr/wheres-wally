// import { render, screen } from '@testing-library/react';
// import App from '../App';

// test("renders Where_is_Wally <p>", () => {
//   render(<App />);
//   const linkElement = screen.getByText(/Where_is_Wally/i);
//   expect(linkElement).toBeInTheDocument();
// });
